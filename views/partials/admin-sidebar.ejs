<div class="flex min-h-screen bg-gray-100">
    <aside class="w-64 bg-slate-900 text-white flex-shrink-0 hidden md:flex flex-col">
        <div class="p-6">
            <h2 class="text-2xl font-black tracking-tighter">ADMIN PANEL</h2>
            <p class="text-xs text-slate-400 mt-1">Manage your contents</p>
        </div>
        
        <nav class="flex-1 px-4 space-y-2 mt-4">
            <a href="/admin/articles" class="flex items-center space-x-3 p-3 rounded-xl hover:bg-slate-800 transition group <%= (req.path === '/admin/articles') ? 'bg-blue-600' : '' %>">
                <span class="font-bold">Manajemen Artikel</span>
            </a>
            
            <a href="/admin/videos" class="flex items-center space-x-3 p-3 rounded-xl hover:bg-slate-800 transition group <%= (req.path === '/admin/videos') ? 'bg-blue-600' : '' %>">
                <span class="font-bold">Manajemen Video</span>
            </a>
        </nav>

        <div class="p-4 border-t border-slate-800">
            <a href="/dashboard" class="flex items-center space-x-3 p-3 text-slate-400 hover:text-white transition">
                <span>🏠</span>
                <span class="text-sm font-medium">Ke Dashboard User</span>
            </a>
            <a href="/api/auth/logout" class="flex items-center space-x-3 p-3 text-red-400 hover:text-red-300 transition">
                <span>🚪</span>
                <span class="text-sm font-medium">Logout</span>
            </a>
        </div>
    </aside>

    <main class="flex-1 h-screen overflow-y-auto">
        <header class="bg-white border-b p-4 flex justify-end items-center px-8">
            <div class="flex items-center space-x-3">
                <span class="text-sm font-medium text-gray-600">Halo, <%= user.fullName %></span>
                <div class="w-8 h-8 bg-blue-500 rounded-full flex items-center justify-center text-white font-bold text-xs">AD</div>
            </div>
        </header>